{
  "name": "nr01-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd ../frontend && npm install && npm run build",
    "build:backend": "echo 'Backend já está pronto'",
    "postinstall": "npm rebuild sqlite3"
  },
  
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "html-pdf": "^3.0.1",
    "jsonwebtoken": "^9.0.2",
    "pdfkit": "^0.19.1",
    "pdfmake": "^0.3.11",
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
